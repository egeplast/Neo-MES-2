import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.egeplast.NeoMES2',
  appName: 'NeoMES2',
  webDir: 'dist/neo-mes-2-frontend/browser',
};

export default config;
